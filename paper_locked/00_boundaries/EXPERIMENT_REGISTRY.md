# Experiment registry and reuse boundaries

`SUCCESS` means only that the stated local acceptance test passed. It never
means that the 2024 converter, startup, ZVS, loss or hardware has been fully
reproduced.

| Experiment | Status | What it established | May be reused for | Must not be used to claim |
|---|---|---|---|---|
| Step 01 analytical audit | `PASS_EQ1_EQ2_EQ3 / CONFLICT_EQ4_TABLE1` | Duty, on-time and peak-current equations transcribed; inductance conflict exposed | Locked analytical inputs and two-branch L tests | Circuit, startup or ZVS reproduction |
| Step 02 topology/mode lock | `DOCUMENTED / PUBLICATION_SPICE_BLOCKED` | Canonical nodes and P24/P25 mode sequence | Netlist connectivity and state names | Numeric commutation timing |
| Step 03 source scan | `CANDIDATES_CLASSIFIED` | Startup, commutation and controller source roles | Selecting detachable candidate modules | Treating cross-topology values as P24 data |
| R00 zero start | `FAILED_ZERO_START_BASELINE` | Direct zero-state fixed PWM is not justified | Negative baseline only | Any electrical success |
| R01 family | `FAILED/INTERRUPTED_CONTROLLER_MODELS` | Instantaneous comparators chatter; several startup paths were unsupported | Numerical/controller lessons only | Power-stage failure or successful startup |
| R02A | `FAILED_PRECHARGE_ESTABLISHMENT` | Unmodified IPEC divider does not establish the required four-level ladder | Negative transfer result | P24 startup conclusion |
| R02B | `COMPLETED_FEASIBLE_TREND / NO_PASS` | Passive-divider capacitance/ramp sensitivity changes physically generated ladder | Choosing an explicitly labelled exploratory precharge point | P24 capacitor/ramp values or startup proof |
| R02C | `PASS_TIMING_LAW / NOT_HARDWARE_VALIDATED` | Literature active-current precharge timing equation behaves as expected | Equation-level startup module | Four-capacitor P24 implementation |
| R03A | `FAILED_FIXED_PWM_TAKEOVER / NUMERICALLY_COMPLETED` | Physically generated ladder survives briefly, but fixed PWM accumulates current | Motivation for boundary-aware takeover | Steady operation or safe current |
| R03B | `FAILED_NUMERICAL_ZCD_CHATTER` | Instantaneous algebraic ZCD is not executable | Requirement for state memory | Electrical behavior |
| R03C | `FAILED_NUMERICAL_COMMUTATION` | Ideal-diode substitution does not close the full commutation problem | Negative numerical lesson | Physical low-side operation |
| R03D | `PARTIAL_SUCCESS_OUTPUT / FAILED_CURRENT_BOUNDARY` | EPE2019 duty ramp controls output and source shock | Soft-start concept only | Phase-current safety, ZVS or P24 reproduction |
| R04A | `PASS_LOCAL_STATE_SEQUENCE / MODULAR_REGRESSION_RECONFIRMED` | Latched sequence works; Eq.(4) L gives 125 A, while Table-I L gives 68.5 A; modular rerun reproduced both | P24 local sequence, state-memory pattern and recalculated 1.4667-nH main value | Propagating the 2.68-nH table discrepancy, four-phase balance, startup or ZVS |
| R04B/R04B-LONG | `REJECTED_NON_P24_COMPLEMENTARY_PWM_BASELINE` | Ordinary per-leg complementary PWM can show near-1-V output while FC charge and phase currents remain unbalanced | Numerical anti-pattern only | Any P24 electrical conclusion, valid steady state, balance, startup or ZVS |
| R05A | `QUARANTINED_CROSS_PAPER_EXPLORATION` | Under its stated P25-assisted sequence and external Coss assumptions, the local threshold lies between 9.4% and 9.5% | Numerical lessons and later P25-branch comparison only | P24 sequence reproduction, startup, periodic balance, loss, or any judgment of P24's 1%-2% statement |
| A40 analytical audit | `PASS_ANALYTICAL_BOUNDARY / MISSING_MODE5_TIME_BUDGET_AND_CAP_PARTICIPATION` | P24 125 A is the zero-admission lossless rise; P25 Eq. (13) gives 7.392/3.696 ns for 385 pF at P24 1%/2%, but no unique C ceiling without an allowed Mode-5 time | Defining the A41 snubber-sweep axes, direction and acceptance metrics | Unique CH/CL values, complete four-phase ZVS, periodic closure or hardware behavior |
| A41 local snubber sensitivity | `28/28 NUMERICALLY COMPLETE / 0 ZVS / 28 GUARD_PASS` | At exact P24 1%-2% release, 0-2 nF positive added capacitance does not create high-side ZVS; it monotonically raises minimum post-release Vds and delays its minimum | Rejecting a wider passive-capacitance search; defining a zero-snubber negative-current threshold experiment | P24 periodic reproduction, hardware snubber need, four-phase closure, startup, or unpublished device values |
| A42 zero-snubber current threshold | `LOCAL_THRESHOLD_BRACKETED_7.76_TO_7.77_PERCENT` | With A41's zero-snubber state fixed, 7.76% leaves 13.924 mV and 7.77% first reaches Vds=0 after 2.155 ns | Separate P25-device-augmented mechanism branch and a concrete capacitance/path question for Mihai | Replacing P24 1%-2%, P24 periodic reproduction, full handoff, startup, loss, or hardware threshold |
| A43 7.77% full-machine transplant | `LOCAL_EVENT_PROGRESS / H2_ZVS_FAIL / GUARD_PASS` | With every A37 state coordinate and rule fixed, 7.77% reaches P1_M5 but H2 Vds bottoms at 1.418 V; the machine blocks H2 and all downstream phases | Demonstrating that A42's local threshold is state-dependent; defining a fixed-state 7.77%-9% H2 boundary scan | A periodic state, P24 1%-2% operation, complete handoff, startup, or hardware behavior |
| A45 EPC2067 Table-3 candidate commutation threshold | `SENSITIVITY_ONLY / LOCAL_THRESHOLD_BRACKETED_22.04_TO_22.05_PERCENT` | Re-running the full interval-1->2->3 chain with 2024 Table 3's own nP=4/nM=4 EPC2067 candidate device (2 HS/3 LS parallel, CH_TOTAL=3720 pF, CL_TOTAL=5580 pF, Ron frozen at the GS61008T value) and its own re-derived interval-2 handoff state moves the local zero-snubber ZVS threshold to 22.04%-22.05%, about 2.8x above A42's GS61008T-based 7.76%-7.77%; the 1%-10% A42-convention grid alone did not bracket it and had to be widened, and the direction (bigger Coss needs more negative current) matches physical expectation | A second, explicitly device-labelled local-threshold sensitivity branch and a concrete "which device did Sec. II-B actually use" question for Mihai | Confirming EPC2067 as the Sec. II-B ZVS-mechanism device, replacing A42, validating an EPC2067 Ron/conduction model, P24 1%-2% operation, four-phase closure, periodic state, startup, or hardware behavior |

| A46 cross-experiment commutation-capacitance scaling | `SENSITIVITY_ONLY`, `R2=0.9469`, `p=0.5117+/-0.0118` | A committed, rerunnable regression (`fit_cross_experiment_scaling.py`) over A41+A42+A45's own already-committed rows (102 points) fits `Vds_min=V0-k*Ineg/Ceff^p`; the free exponent lands at 0.5117+/-0.0118, essentially the naive resonant-energy p=0.5. Stated explicitly to avoid an earlier ambiguity: the *threshold* `Ineg_needed` scales as `Ceff^{+p}` (increasing with capacitance), not `Ceff^{-p}` -- only the achieved voltage swing at fixed `Ineg` falls as `Ceff^{-p}`. The global fit reproduces A42's and A45's own directly-measured brackets to within +3.3%/+10.0%, not exactly. | Citing that the three device/capacitance sensitivity results are mutually consistent with one constant-capacitance model, to within the stated accuracy | Any P24 hardware threshold, nonlinear Coss(V) validation, which device Sec. II-B actually uses, four-phase closure, startup, or treating the model as more accurate than its 3-10% deviation from the two experiments' own direct measurements |
| A47 nonlinear GS61008T Coss(V) local ZVS threshold | `SENSITIVITY_ONLY / LOCAL_THRESHOLD_BRACKETED_9.63_TO_9.64_PERCENT` | Re-ran the full interval-1->2->3 chain for the SAME GS61008T device/Ron A42 used, replacing its constant `Co(tr)` commutation capacitance with a real, digitized (not fabricated) nonlinear `Coss(V)` curve read from the GS61008T datasheet's own Figure 7 (Rev 200402, calibrated against the plot's own axis labels, cross-checked to within 1-3% against the datasheet's own printed `Coss(50V)`/`Co(er)`/`Co(tr)` values; raw points and fit committed). The local zero-snubber ZVS threshold moves to 9.63%-9.64%, about 1.24x ABOVE A42's constant-capacitance 7.76%-7.77% -- the threshold *increases*, moving further from P24's stated 1%-2%, not closer, because the digitized curve's near-`Vds=0` capacitance (~702 pF/device) is itself larger than the constant `Co(tr)`=385 pF it replaces. This refutes the motivating hypothesis (that nonlinear Coss(V) might explain A46's 4-17pF-vs-257pF gap) for this specific device's own datasheet curve. LTspice's native `Q=` nonlinear-capacitor device and a naive single-B-source `ddt()` construction both failed to converge in this stiff capacitor-loop topology; a floor-capacitor-plus-bounded-correction `ddt()` construction with `method=gear` and an adaptive per-row tolerance escalation (`run_a47_sweep.py`) was needed instead. | A same-device, capacitance-model-only sensitivity branch showing nonlinear Coss(V) moves the threshold the "wrong" direction for GS61008T; a validated LTspice nonlinear-capacitor construction pattern for future device-Coss work | Confirming this as validated GS61008T hardware behavior (digitization carries 1-6% cross-checked uncertainty, larger and unresolved below ~5V where COSS is graphically fused with CISS in the source figure), any EPC2067 or other-device nonlinear-Coss conclusion (not tested here), P24 1%-2% operation, four-phase closure, periodic state, startup, or hardware behavior |
| A48 dead-time feasibility sweep | `EXPECTED_FAILURE` (1%/2% rows), `LOCAL_PASS` (7.77% narrow window), `REJECTED` ("any T_DEAD above natural duration is safe" hypothesis), `SENSITIVITY_ONLY` (overall) | Replaced A42/R04D3A's ideal Vds=0 event-detector turn-on with a fixed-delay (`T_DEAD`) turn-on on the same P24 t2->t3 local chain and A42's own fixed rows (1%, 2%, 7.77%). 1%/2% never reach near-zero residual switching voltage at any `T_DEAD` in 0.5-20 ns (dead time cannot manufacture missing negative-current margin). The 7.77% row reproduces A42's natural-ZVS result only inside a narrow window, `T_DEAD~=2.04-2.28 ns`, centered on A42's own independently-measured 2.1516 ns commutation duration (exact zero found at 2.15 ns, cross-validating A42); outside that window residual voltage does not stay low or improve monotonically -- it swings non-monotonically up to 20.9 V because the LC tank keeps ringing with QH1 off. A `chgtol=1e-16` (A42's original) hangs indefinitely on the genuinely hard-switching cases this experiment adds; relaxed to LTspice's default `1e-14`, verified not to change the shared pre-event dynamics (reproduces A42's own 9.2566 V 1%-row minimum to 4 significant figures). | Citing the GS61008T/7.77% dead-time bracket as a feasibility map to compare against Mihai's real dead-time number once given; citing the rejected "any T_DEAD above natural duration works" hypothesis as a documented anti-pattern | The real dead time/driver propagation delay (still `UNKNOWN_BLOCKING`/unanswered from Mihai), any claim that 1%/2% negative current becomes ZVS-viable at any dead time, four-phase closure, periodic state, startup, hardware switching-loss reproduction, or the EPC2067/A45 device branch (not swept here) |

| R04E5 ramped-duty event-gated zero start | `CONTROLLER_GUARD_PASS` (12/12 cells), `SENSITIVITY_ONLY` (overall grid) | Two-parent synthesis of R03D's ramped on-time ceiling (0 to locked P24 Ton over TSOFT) grafted onto R04E3's event-driven latched per-phase current-limited controller, swept over TSOFT in {50,100,200,500} us x I_LIMIT in {10,50,150} A (12 cells) on the full one-module/four-phase P24 stage (phase 1 active only, true zero energy, L=1.4666667 nH). In all 12 cells the machine never returns to ENERGY a second time -- it parks permanently at R04E3's own stuck point (state 4, waiting for high-side Vds=0; 9/12 cells) or one stage earlier (state 3, waiting for the negative-current target; the 3 I_LIMIT=150 cells), now shown to persist to at least 600 us (30-370x longer than R04E3's original 20 us test). Because of this, TSOFT has no observed effect on any measured quantity in the grid; I_LIMIT sets the single first-pulse's peak current (14.2/65.5/155.7 A, all safely inside the +/-250 A bound adopted) but does not help Vout bootstrap. Vout never approaches 1 V in any cell (RC-decays back toward zero after one pulse). This does not exceed either parent and does not recover R03D's Vout success, with a stated causal reason: R03D's success depended on a fixed-time PWM schedule that this project's boundary-control principle prohibits, and once physical-event gating is enforced (as both R04E3 and R04E5 correctly do), no further pulse can be issued without the gating event actually occurring | Citing that neither the ramp-duration axis nor the current-limit axis alone resolves R04E3's own bottleneck; citing the confirmed persistence of R04E3's stuck point out to 600 us as a stronger version of R04E3's own 20-us finding | Any P24 startup sequence, periodic closure, four-phase interleaving beyond the phase-1 startup transient, a "correct" TSOFT or I_LIMIT value (both are sensitivity axes only), or hardware behavior |

| R04E6 EPE2019 charge-redistribution ladder bootstrap | `NOT_CONVERGING` (9/16 cells, NCYC>=3), `SENSITIVITY_ONLY` (7/16 cells, NCYC=1) | `CROSS_PAPER_EXTENSION` from EPE2019 (Roberts/McRae/Prodic, already an approved Cfly/Cout source in this project): the conventional-4-phase-SC-buck 3-state charge-redistribution startup sequence (Fig. 5) -- state (a) charges C1 alone via H1+L1, state (b) redistributes C1<->C2 via H2+L1+L2, state (c) redistributes C2<->C3 via H3+L2+L3 (independently derived from this project's own P24 topology and cross-checked switch-for-switch against a fresh high-DPI render of the source figure, correcting an earlier, wrong paraphrase that had assumed H1+L4 series charging) -- run open-loop on true-zero-energy P24 (Cfly=53.8 uF unchanged), sweeping hold time TH in {200ns,1,5,20us} x cycle count NCYC in {1,3,5,10}. A single, fully-settled cycle (TH>=5us, NCYC=1) reaches VC1~24V/VC2~VC3~12V (LADDER_ERR=0.836, vs. R02B's own best 0.260), moving partway from zero without touching the inductor/Vout timing that blocked every R03/R04E0-5 attempt; every additional cycle beyond the first monotonically increases LADDER_ERR at every hold time >=1us (by NCYC=10 all three capacitors converge toward a common ~45-46V, the opposite of the required 3:2:1 asymmetry) because states (b)/(c) are unbiased pairwise equalizers with no mechanism favoring the target ratio. A bit-identical 1.23e6 A "ICS1_MAX" across all 16 cells was caught as a sub-1ns numerical startup-transient artifact (present identically regardless of swept parameters) and excluded; properly time-windowed currents (hundreds to ~3.4 kA) were used instead. | Citing that this specific literal (fixed-hold-time, fixed-cycle-count, unmonitored) form of the EPE2019 mechanism does not reach 36/24/12V and gets worse with more cycles, as the reason a future attempt needs a voltage-monitored/gated version instead; citing the single-cycle TH=20us result as the best starting point found so far for any bootstrap-only sub-problem | That this mechanism (in this tested form) solves ladder bootstrap, that more cycles ever help, any Vout/steady-state handoff (out of scope here), hardware switch-stress ratings (not checked), or that a monitored/gated variant of the same states would behave the same way (untested) |

| R04E7 voltage-ratio-gated EPE2019 charge-redistribution ladder bootstrap | `LOCAL_PASS` (3/3 cells) | Single conceptual change from R04E6 (same EPE2019 Fig. 5 truth table, power stage, Cfly/Cout, reused unchanged): R04E6's blind fixed-hold-time/fixed-cycle-count PWL gating is replaced by voltage-RATIO gating implemented in this project's own `.machine`/`.state`/`.rule` construct -- state (a) exits at `V(C1)>=36V` (final target, direct), state (b) exits at `2*V(C1)<=3*V(C2)` (ratio 3/2, self-correcting), state (c) exits at `V(C2)<=2*V(C3)` (ratio 2/1), the full cycle repeats until all three voltages are simultaneously within a swept tolerance band or a fixed 50us global time safety cap is hit. A charge-conservation check (done before building anything) confirms a single pass cannot reach 36/24/12V with equal Cfly and Vin=48V (would require charging C1 to 60V), so multiple cycles are structurally required even in the fixed design -- confirmed directly (fastest cell still took 5 cycles). Swept TOL in {2%,5%,10%} (TCAP_TOTAL=50us fixed, not swept): ALL 3 cells reached DONE well inside the cap (t=3.90-4.24us, 5-8 cycles), with LADDER_ERR decreasing monotonically on EVERY single cycle (1.6->0.96->0.576->0.346->0.207->0.124->0.075->0.045 in the TOL=2% cell) -- the opposite of R04E6's own finding. Final LADDER_ERR: 0.207 (TOL=10%), 0.124 (TOL=5%), 0.045 (TOL=2%) -- all three beat R02B's 0.260 and R04E6's 0.836 best. No cell hit the safety cap without converging. No comparator chatter observed (reasoned and confirmed: voltage-based comparators on continuous capacitor states are structurally less chatter-prone than R03B's current-based one; clean monotonic per-cycle timestamps, 117-135s real run time per cell, no hysteresis workaround needed). Peak capacitor-to-capacitor currents remain in the hundreds-to-kilo-amp range (e.g. ICS1 up to ~4567A), same qualitative flag as R04E6, no numeric threshold applied (source gives none). | Citing that voltage-ratio gating (vs. blind timing) is the mechanism that fixes R04E6's diagnosed equalization-drift failure, for any future EPE2019-mechanism bootstrap work; citing the TOL=2% cell's 35.60/23.60/11.80V, LADDER_ERR=0.045 result as the best zero-start bootstrap-only ladder result in this project so far, and as a candidate seed for a future handoff study | Any P24 Vout/steady-state handoff (not attempted), hardware switch-stress validation of the multi-kilo-amp currents, four-phase interleaving (phase 4 never driven), that this specific TOL/TCAP_TOTAL choice is "correct" rather than a sensitivity/engineering choice, or general chatter-immunity outside this experiment's own 3-cell grid |

| R04E8 corrected-Cfly voltage-ratio-gated EPE2019 charge-redistribution ladder bootstrap | `LOCAL_PASS` (3/3 cells) | Module swap inside R04E7's unchanged framework (same truth table, power stage, ratio-gating `.machine` construct, Cout=4.672mF unchanged/not corrected here -- separate open gap): CFLY swept in {1,3,8.7 uF} -- representative low/mid/high points spanning `CFLY_FIRST_PRINCIPLES_ESTIMATE.md`'s derived `~0.6-8.7 uF` first-principles range (replacing R04E7's `53.8 uF`, found to have a cross-topology provenance problem: that value is EPE2019's own CSC-buck prototype Table I sum, a different, topologically modified converter, whose parts are rated only 35V/50V, not enough for P24's 48V Vin) -- with TOL FIXED at 2% (R04E7's own best-performing cell, for the fairest one-parameter comparison), not swept. A pilot run (Cfly=3uF, untracked) confirmed both physical expectations directly before committing the grid: convergence is dramatically faster at smaller Cfly (RC time constant shrinks with C), while peak capacitor-to-capacitor current does NOT shrink proportionally (V/Ron-dominated, roughly C-independent) -- both checked against data, not assumed. TCAP_TOTAL reduced to 10us and TMAX to 50ps (both post-pilot, scaled to the new, faster timescale; solver-resolution settings only, not physical/not swept). ALL 3 cells reached DONE in exactly 8 cycles each (matching R04E7's own TOL=2% cycle count), with LADDER_ERR landing in a tight 0.0442-0.0448 band, essentially matching R04E7's own 0.045 -- confirming the ratio-gating comparators are scale-invariant in Cfly (they compare voltage ratios/absolute thresholds, not capacitance-dependent quantities). Convergence time scales close to linearly with Cfly (a tight, consistent ~8% super-linear correction across the whole 8.7x tested range: 85.60ns at 1uF, 256.72ns at 3uF, 741.62ns at 8.7uF, vs R04E7's 4239.5ns at 53.8uF -- 5.7x to 49.5x faster). Peak ICS1/ICS2/ICS3 MAX currents stay roughly flat (within ~10-15%) across the whole 1-53.8uF range (e.g. ICS1_MAX 4157.7/4431.5/4525.0/4566.9 A at 1/3/8.7/53.8 uF) -- confirming peak current does not shrink with smaller Cfly; some MIN/reverse currents (ICS3_MIN, IL1) DO scale up substantially with Cfly, a real, checked-not-assumed asymmetry. No cell hit the safety cap without converging. | Citing that R04E7's ratio-gating mechanism (not just its specific 53.8uF numbers) transfers cleanly to the corrected, physically-appropriate Cfly scale, with convergence quality essentially Cfly-independent and convergence speed scaling close to linearly with Cfly; citing that the multi-kilo-amp ICS1/ICS2 currents already flagged in R04E6/R04E7 are NOT resolved by the Cfly correction (peak current stays flat, confirming it is a structural consequence of the zero-dead-time/Ron-only idealized switch model, independent of Cfly) | That any single tested Cfly value (or the 0.6-8.7uF range's own bounds) is the hardware-correct value -- the range itself is a sensitivity/first-principles estimate depending on an unconfirmed ripple-percentage design choice, not a paper value; any Cout correction (deliberately not attempted here, a separate unresolved gap); any P24 Vout/steady-state handoff; hardware switch-stress validation of the multi-kilo-amp currents; four-phase interleaving (phase 4 never driven); or general chatter-immunity/scaling behavior outside this experiment's own 3-cell grid |

| R04E9 unified inductor-mediated four-phase bootstrap | `CONTROLLER_GUARD_PASS` (9/9 cells), `SENSITIVITY_ONLY` (overall grid) | Multi-parent synthesis on the full four-phase P24 stage, true zero energy, ALL four phases actively commanded (not one phase held statically as every prior R04E experiment did): a unified rotating `CHARGE_k`/`FREE_k` machine per phase `k=1..4`, where `CHARGE_k` (QHk on) exits at R04E3/R04E4's own validated current-limit rule `I(Lk)>=I_LIMIT`, and `FREE_k` (QLk on) exits at the earlier of the natural `I(Lk)<=0` event or an A48-style timeout `T_FREEWHEEL_MAX` (event-OR-timeout). Unlike R04E6/R04E7/R04E8's borrowed EPE2019 switch-only charge-redistribution mechanism, every phase's charging current is routed through its OWN series inductor `Lk` -- P24's own Interval-1 mechanism, not a bare switch-to-switch capacitor path. `CFLY=3uF` (R04E8's own corrected middle value, not R04E7's cross-topology-suspect 53.8uF); `COUT=4.672mF` unchanged/flagged-unconfirmed. Swept `I_LIMIT` in {10,30,60}A x `T_FREEWHEEL_MAX` in {50,200,1000}ns (9 cells), `TSTOP=3us`/`TMAX=50ps` fixed, pilot-justified (bit-identical results at 3us vs 20us extension, confirming a genuine equilibrium, not truncation). Two construct bugs were found and fixed during piloting before any grid cell was committed: a missing timer storage capacitor, and an inverted B-source current sign that silently defeated the timeout branch entirely in the first (discarded) pilot -- flagged as a latent, never-exercised risk in R04E5's own analogous timer construct, not fixed there (out of scope). With both fixed, ALL 9 cells advance through `CHARGE1->FREE1->CHARGE2` within ~1us and then permanently stall in `CHARGE2`: `I(L2)` never reaches its own commanded `I_LIMIT` in any cell (best case 67% of the way, `I_LIMIT=60A`/`T_FREEWHEEL_MAX=50ns`), because phase 2's only drive is `C1`'s own limited charge from phase 1's single `~0.93ns` pulse -- there is no direct `Vin` path to phase 2, unlike phase 1's own direct `SH1` connection. Zero full rotations complete in any cell; the handoff condition (`Vout` within 5% of 1V and `VC1-3` within 2% of 36/24/12V) is never reached. Both swept axes have real, monotonic, opposite-direction effects on how close `IL2_max` gets to `I_LIMIT` (larger `I_LIMIT` and shorter `T_FREEWHEEL_MAX` both raise `IL2_max`, the latter because less-decayed residual `IL1` couples more strongly into phase 2) -- unlike R04E5, where the swept `TSOFT` axis had NO observed effect at all. Peak currents everywhere in the grid stay under 61A (`IL1_max`) and under 36A (`ICS1_max`) -- roughly two orders of magnitude below R04E6/R04E7/R04E8's 4.2-4.6kA figures, directly confirming that inductor-mediated charging is far more physically plausible than the switch-only mechanism, though it does not (in this tested grid) reach the handoff condition either. | Citing that inductor-mediated (not switch-only) phase charging produces peak currents roughly two orders of magnitude smaller than R04E6/E7/E8's mechanism, a structural result independent of the specific `I_LIMIT`/`T_FREEWHEEL_MAX` values tested; citing the CHARGE2 stall's specific cause (no direct `Vin` path to phase 2, only C1's limited relayed charge) as the next module to address (e.g. multi-pulse phase-1 precharge before allowing rotation to phase 2); citing the flagged B-source sign-convention risk for any future elapsed-time-timer construct in this project, including a review of R04E5's own (never-exercised, so not directly falsified) analogous construct | Any P24 Vout/steady-state handoff (not attempted -- only the handoff CONDITION is measured, never built or exercised), four-phase periodic/interleaved operation (no cell completes even one full rotation), any tested `I_LIMIT`/`T_FREEWHEEL_MAX`/tolerance-band value as "correct" (all are sensitivity/engineering choices), hardware switch-stress validation of the reported currents, a `Cout` correction (not attempted, same open gap as R04E7/R04E8), or that R04E5's own committed results are invalidated (R04E5's swept axis never exercised the affected timer branch, so its results are not directly impacted) |
| R04E10 timeout-gated multi-rotation bootstrap | `CONTROLLER_GUARD_PASS` (9/9 cells: 6/9 with genuine multi-rotation progress, 3/9 reproduce R04E9's own total stall), `SENSITIVITY_ONLY` (overall grid) | Single conceptual change from R04E9 (same unified rotating `CHARGE_k`/`FREE_k` machine, full four-phase P24 connectivity, `CFLY=3uF`, `COUT=4.672mF`, current-limit mechanism, `FREE_k` timer all reused unchanged): `CHARGE_k` now ALSO exits at a new timeout `T_CHARGE_MAX` (OR'd with the existing `I(Lk)>=I_LIMIT` rule), symmetric with `FREE_k`'s own existing event-OR-timeout pattern, via an exact mirror of R04E9's own validated `FREE_k` timer construct (charge/reset roles swapped). An isolated pilot (current-limit branch disabled) found `T_CHARGE_MAX` values near R04E9's own ~0.93ns phase-1 pulse width (100ps, 1ns) break the construct (a spurious backward state transition, confirmed via raw-trace inspection), while `{5,20,50}ns` were clean in isolation -- so the grid is restricted to that pilot-verified-safe range. `T_FREEWHEEL_MAX` fixed at R04E9's own best-performing value (50ns, not re-swept). Swept `I_LIMIT` in {10,30,60}A x `T_CHARGE_MAX` in {5,20,50}ns (9 cells), `TSTOP=20us`/`TMAX=50ps` (extended from R04E9's 3us to allow multiple rotations). RESULT: 6/9 cells (all `T_CHARGE_MAX in {20,50}ns`) complete 4-17 full rotations with `Vout` rising monotonically across every completed rotation in every one of those 6 cells -- a genuine improvement over R04E9's 0/9. The other 3/9 cells (`T_CHARGE_MAX=5ns`, ALL THREE `I_LIMIT` values) complete ZERO rotations, reproducing R04E9's own total stall exactly, DESPITE this exact `T_CHARGE_MAX` value passing isolated verification -- a genuine, reportable finding that isolated single-branch pilot verification does not guarantee clean behavior once both OR-branches are live in the full circuit-coupled machine. No cell reaches handoff; the best cell (`I_LIMIT=60A`,`T_CHARGE_MAX=50ns`, 12 rotations) reaches `Vout=1.41%`, `VC1=5.51%`, `VC2=2.58%`, `VC3=0.73%` of target -- `3x-20x` further than R04E9's own best single-pass cell, but still 95-99% short. `VC2` specifically regresses (net negative, wrong direction) in 3 of the 6 multi-rotation cells (all at `T_CHARGE_MAX=20ns`), while progressing correctly at `T_CHARGE_MAX=50ns` in all three `I_LIMIT` values -- a real, reproducible, axis-dependent split. Inductor currents (`IL1-4`) stay bounded and physically plausible (max `60.17A`, same order as R04E9, comfortably inside the +/-250A safety bound). However, a NEW, pervasive numerical artifact contaminates flying-capacitor branch current (`ICS1-3`) `.meas` reporting in EVERY ONE of the 9 cells (hundreds of A up to ~500kA), diagnosed directly (raw-trace inspection: repeated identical timestamps, unchanging VC/IL values, non-integer "state" value) as a solver-convergence artifact tied to a newly-found retry/chatter dynamic (the machine repeatedly makes partial forward progress then reverses to FREE1 before eventually completing a rotation, causing the first rotation in a representative cell to take 18.86us -- 70x longer than a naive per-state-duration estimate -- while later rotations complete cleanly in 273-347ns matching that estimate) -- NOT present in R04E9's simpler single-timer construct. Compared against R04E7/R04E8's switch-only ladder mechanism (which reached `VC1~=35.6V`, 98.9% of target, in 5-8 cycles): R04E10 needs MORE cycles (11-17) to reach far LESS (3-5.5% of target), confirming inductor-mediated charging trades convergence speed for physical plausibility on the inductor branches (though the new capacitor-current artifact undercuts part of that plausibility claim for the `ICS1-3` branches specifically). | Citing that a `CHARGE_k` timeout fallback (added symmetrically with `FREE_k`'s own existing pattern) genuinely unlocks multi-rotation operation and monotonic `Vout` progress where R04E9's single-pass-only construct could not, for `T_CHARGE_MAX` values well above the ~1ns pulse-width scale; citing the `T_CHARGE_MAX=5ns`-fails-despite-isolated-verification finding as a methodological lesson for any future timer-construct experiment in this project (isolated single-branch pilot verification is necessary but not sufficient); citing the newly-found retry/chatter dynamic and its associated `ICS1-3` solver artifact as an open problem for any future experiment reusing this dual-timer OR-construct pattern; citing the `VC2`-axis-dependent-direction split as a candidate for a dedicated follow-up sweep | Any P24 Vout/steady-state handoff (not attempted -- only the handoff CONDITION is measured); that any tested `I_LIMIT`/`T_CHARGE_MAX` value is "correct" (all are sensitivity/engineering choices, and `T_CHARGE_MAX=5ns`'s clean isolated behavior explicitly does NOT generalize to the full machine); that this is full four-phase INTERLEAVED steady-state operation (still a bootstrap-only transient study); the `ICS1-3` current magnitudes reported by this grid's own `.meas` output as physical results (flagged as solver-artifact-contaminated in every cell); hardware switch-stress validation of the `IL1-4` currents; a `Cout` correction (not attempted, same open gap as R04E7/R04E8/R04E9) |

| R04E11 charge/free reversal root-cause diagnosis | `CONTROLLER_GUARD_PASS`-style partial success (diagnostic), `SENSITIVITY_ONLY` (overall) | Diagnostic-only (no mechanism change; `.save`-only instrumentation added to two of R04E10's own already-committed, already-run cells: `I_LIMIT=30A` at `T_CHARGE_MAX=20ns` -- the primary target, and `T_CHARGE_MAX=5ns` -- one of the three permanently-stalled secondary targets). Directly tested two hypotheses for R04E10's own undiagnosed `CHARGE_k`/`FREE_k` reversal: (1) the two reset-gate booleans (`reset_gate`/`reset_gate_chg`) briefly disagreeing during a mid-transition `state_mon` value -- REFUTED directly (0/231 conflicts across 151+80 sampled rows spanning two independent reversal/artifact episodes in the two cells; the gates are perfect complements of `V(state_mon)` at every sampled row in both cells). What direct row-by-row raw-trace inspection actually shows: the raw `.machine` state variable itself (`V(state_mon)`) sweeps continuously and monotonically BACKWARD through multiple half-integer bucket boundaries within picoseconds (e.g. `3.9999995->3.4999995->2.4999998->1.4999998` in ~16ps, confirmed at the row level), during the same class of genuinely stiff, sub-picosecond-timestep solver-retry episode that separately produces the `ICS1-3` current-magnitude artifact R04E10 also flagged -- both are manifestations of the same underlying solver difficulty, not two independent problems. Exact, reproducible retry periods were measured directly (`143.14ns` and `214.71ns`, refining R04E10's own "~143ns" estimate to 4+ significant figures; 113 reversals total in the escaping `T_CHARGE_MAX=20ns` cell vs. 273 spread across the ENTIRE run in the permanently-stalled `T_CHARGE_MAX=5ns` cell, confirming the same dynamic in more severe form). (2) A minimal, well-motivated candidate fix -- giving the reset switches (`SRESET`/`SRESET_CHG`, model `SWRESET`, previously `Vt=2.5V, Vh=0` -- zero hysteresis, sitting exactly on the SAME half-integer boundaries the `.machine` itself uses) explicit hysteresis (`Vh=1`) -- was built and run as a controlled isolation test on the primary cell. RESULT: bit-identical to the unmodified baseline at every checked level (same row 654331, same 15-significant-figure timestamp `1.9275359627109638e-05`, same `V(state_mon)=3.217076301574707`, same final state/voltages/rotation timestamps) -- directly REFUTING the reset-switch-hysteresis hypothesis as a working fix. One further finding: R04E10's own `BOUNDARY.md` Section 5b cited `t=19.275us` as "one concrete reversal event"; direct inspection shows this exact instant is actually a CLEAN forward FREE2->CHARGE3 transition coincident with the `ICS2`/`ICS3` artifact and the non-integer `V(state_mon)=3.217` -- both of those sub-findings are fully reproduced and confirmed, but the "reversal" label on this specific timestamp is corrected, not confirmed. No fix that changes the outcome was found; per Ground Rule 7 none is forced onto the construct, and the 9-cell grid is NOT re-run (R04E10's own grid/`results.csv`/`results.json` remain the authoritative record for all 9 cells, unchanged by this experiment). | Citing that the two-reset-gate-race hypothesis for this construct family's reversal dynamic is directly refuted by raw-trace evidence, not merely unconfirmed; citing that the reversal and the `ICS1-3` artifact are correlated symptoms of the same underlying stiff-solver-episode class (though not always coincident at the same exact instant, per the `t=19.275us` finding); citing the two exact, reproducible retry periods (`143.14ns`, `214.71ns`) as a refinement of R04E10's own approximate figure; citing that a zero-hysteresis reset-switch comparator sitting on the machine's own natural state boundaries is NOT, on direct controlled-isolation-test evidence, the cause of the reversal, for any future contributor considering that same fix; citing the corrected characterization of R04E10's own `t=19.275us` timestamp as a forward transition, not a reversal, while leaving R04E10's own `ICS`/non-integer-state_mon finding at that timestamp intact | That this experiment identifies a fix (none was found to work); any claim that the raw `.machine` state-variable excursion is fully understood at the LTspice-internals level (only its correlation with stiff sub-picosecond-timestep episodes and its independence from this project's own gate/switch logic are established); any change to R04E10's own 9-cell grid, `results.csv`/`results.json`, or grades (unchanged, not re-run); any P24 Vout/steady-state handoff; any `Cout` correction; general reliability of LTspice's `.machine` construct beyond this project's own specific dual-timer usage pattern |

| R04E12 phase-1 precharge admission gate | `CONTROLLER_GUARD_PASS` (5/5 cells), `SENSITIVITY_ONLY` (overall, mixed/non-monotonic improvement finding) | Single conceptual change from R04E10 (same reused, unchanged `CHARGE_k`/`FREE_k` current-limit-OR-timeout construct, full four-phase P24 connectivity, `CFLY=3uF`, `COUT=4.672mF`, `I_LIMIT=60A`, `T_CHARGE_MAX=T_FREEWHEEL_MAX=50ns` all fixed at R04E10's own best-performing cell): a precharge admission gate is inserted before the machine's normal round-robin rotation, implementing R04E9's own `RESULTS.md` Section 11 option (c) verbatim per explicit user direction -- a counter tracks how many times `FREE1` has completed; while below `N_PRECHARGE`, `FREE1`'s exit routes back to `CHARGE1` (repeat phase 1 alone) instead of advancing to `CHARGE2`; once the counter reaches `N_PRECHARGE`, the machine joins R04E10's own unchanged round-robin permanently. Because this project's own `.machine` convention never uses more than one `.rule` per state anywhere in R04E5-R04E11 (confirmed directly, not assumed), the counter was implemented as a compile-time-unrolled chain of `N_PRECHARGE-1` extra state pairs electrically mirroring `CHARGE1`/`FREE1` (one generated netlist per grid cell, this project's own established convention) rather than a runtime SPICE counter node -- a documented implementation-detail choice within `BOUNDARY.md`'s own stated latitude, not a deviation from the requested mechanism. Swept `N_PRECHARGE` in {1,3,5,10,20} (5 cells), `TSTOP=20us`/`TMAX=50ps` unchanged from R04E10. `N_PRECHARGE=1` was confirmed, both by construction and by direct re-run in real LTspice, IDENTICAL to R04E10's own committed `I_LIMIT=60A/T_CHARGE_MAX=50ns` cell to full printed precision on every measured quantity (`STATE_FINAL`, `VOUT_FINAL`, `VC1-3_FINAL`, all `IL1-4`/`ICS1-3` MAX/MIN, rotation count) -- the required baseline sanity check, passed cleanly. The construct itself was pilot-verified by direct raw `.machine` state-trace inspection at `N_PRECHARGE=3` and `=10` (not merely assumed from intended logic): the machine visits every precharge pair exactly once, in strictly increasing order, before ever reaching the real `CHARGE1`, and never re-enters the precharge chain after first reaching `CHARGE2` -- all checks PASS at both tested values. RESULT: `Vout_final` improves monotonically and substantially with `N_PRECHARGE` (`0.0141V` at `N_PRECHARGE=1` to `0.0197V` at `N_PRECHARGE=20`, `+40%`) -- the cleanest result. `VC2_final`, the axis this experiment specifically targeted (R04E10's own weakest-progressing quantity), does NOT improve monotonically: better than baseline at `N_PRECHARGE=3` (`+16%`) and best of the grid at `N_PRECHARGE=20` (`+38%`), but WORSE than baseline at `N_PRECHARGE=5` (`-18%`) and essentially flat at `N_PRECHARGE=10` (`+0.5%`) -- a genuine, honestly-reported mixed/non-monotonic result, not a clean "precharge helps" verdict. A new trade-off was found: the single best-`VC2` cell (`N_PRECHARGE=20`) has the ONLY negative `VC3_final` value (`-0.170V`) anywhere in this grid, where every other cell (including baseline) stays positive. The R04E10/R04E11-diagnosed retry/chatter dynamic and its coincident `ICS1-3` numerical artifact recur in every one of the 5 cells at essentially uniform severity regardless of `N_PRECHARGE` (`19.2-21.8%` of settled-state dwell transitions are reversals in every cell, closely matching R04E10's own `~19%` figure for its representative cell) -- confirming this is a property of the reused `CHARGE_k`/`FREE_k` construct itself, neither caused nor fixed by the precharge stage, and (contrary to a plausible expectation) not concentrated or amplified specifically at the largest tested `N_PRECHARGE=20`. No cell reaches handoff (`T_HANDOFF` FAILs in all 5); `IL1-4` stay safely bounded in every cell (worst case `IL1_max=60.178A`, `24%` of the `+/-250A` safety bound; `IL2_max` shows a small real increase to `34.27A` at `N_PRECHARGE=20`, still `14%` of the bound) -- no `PHYSICAL_BOUNDARY_FAIL` in any cell. | Citing that a compile-time-unrolled state-pair chain is a valid, pilot-verified way to implement a "repeat-N-times-then-advance" admission gate within this project's own `.machine` convention (which never supports more than one `.rule` per state), for any future experiment needing similar sequencing logic; citing that front-loading charge onto `C1` via repeated phase-1-only precharge passes measurably improves `Vout` monotonically and can measurably improve `VC2` at some (not all) tested `N_PRECHARGE` values, as a genuine but non-monotonic partial answer to R04E9/R04E10's own identified `VC2`-lags structural weakness; citing the `N_PRECHARGE=20`/negative-`VC3` trade-off as a concrete example that improving one state variable via this mechanism can measurably cost another, worth checking in any future precharge-based follow-up; citing the uniform `~19-22%` reversal-rate recurrence across all 5 cells as further confirmation (beyond R04E10/R04E11's own cells) that this retry/chatter dynamic is a property of the shared `CHARGE_k`/`FREE_k` timer construct, not of any one grid cell's specific parameters | That `N_PRECHARGE=20` (or any tested value) is "optimal" or paper-derived (the swept axis is `SENSITIVITY_ONLY`, an order-of-magnitude first exploration; the non-monotonic `VC2`/`VC1` results mean a finer sweep between tested points was NOT attempted and could behave differently); that this experiment closes or substantially narrows the handoff gap (`N_PRECHARGE=20`'s best `Vout` is still `98%` short of `1V`, the same structural per-cycle-convergence limitation R04E10 Section 10 already identified, unaddressed here); any P24 Vout/steady-state handoff (not attempted); a `Cout` correction (not attempted, same open gap as R04E7-R04E11); that precharge-admission-gating reliably improves `VC2` in general (`N_PRECHARGE=5` and `=10` directly refute a monotonic-improvement claim); R04E9 Section 11's options (a) (direct topology change) or (b) (passive precharge circuit), neither evaluated here; any further diagnosis of the `ICS1-3` artifact's LTspice-internals root cause (R04E11 already left this undiagnosed at that level; this experiment adds recurrence data only, not a new root-cause finding) |

| R04E13 phase-2 precharge admission gate, stacked on phase-1's own | `CONTROLLER_GUARD_PASS` (5/5 cells), `SENSITIVITY_ONLY` (overall, net-negative-to-marginal finding) | Single conceptual change from R04E12 (same reused, unchanged phase-1 gate fixed at its own clean best `N1_PRECHARGE=3`; full four-phase P24 connectivity, `CFLY=3uF`, `COUT=4.672mF`, `I_LIMIT=60A`, `T_CHARGE_MAX=T_FREEWHEEL_MAX=50ns` all fixed): a SECOND, analogous precharge admission gate is stacked after phase 1's own, gating phase 2 the same way R04E12 gated phase 1 -- repeat `CHARGE2`/`FREE2` `N2_PRECHARGE-1` extra times before ever advancing to the real `CHARGE3`. A naive first implementation attempt (hanging the phase-2 mirror chain directly off the real, round-robin-shared `FREE2` state's own single rule -- the most direct generalization of R04E12's own phase-1 pattern) FAILED its own required pilot verification immediately: because `CHARGE2`/`FREE2` are themselves part of the closed round-robin cycle (a `.machine` where every state has exactly one outgoing rule is a deterministic functional graph, and anything reachable from a cyclic state is also on that cycle forever), the mirror chain re-triggered on EVERY subsequent rotation, not just once -- caught directly by the required pilot check before any result was trusted, not discovered later. The corrected construct uses a dedicated, one-time-only "ADMIT" duplicate of `CHARGE1`/`FREE1`/`CHARGE2`/`FREE2` (electrically identical mirrors), positioned strictly upstream of the closed loop, exactly the structural position R04E12's own phase-1 mirror chain already occupies -- verified two independent ways: a run-independent static rule-graph/deterministic-walk-simulation check (all 5 cells PASS, proving convergence to the intended loop with every precharge/ADMIT state visited at most once) and direct raw-trace pilot verification. Swept `N2_PRECHARGE` in {1,3,5,10} (4 cells) with `N1_PRECHARGE=3` fixed, plus a separate `N1=1,N2=1` identity-check cell (5 netlists total), `TSTOP=20us`/`TMAX=50ps` unchanged. Both required baseline identities confirmed to full printed precision: `N1=1,N2=1` IDENTICAL to R04E10's own committed cell; `N1=3,N2=1` IDENTICAL to R04E12's own committed `N_PRECHARGE=3` cell. Pilot verification at `N2_PRECHARGE=3` PASSED all six checks cleanly; at `N2_PRECHARGE=5`/`10` the strict admission-order pattern-match FAILED, but precisely-timed cross-cell evidence (the SAME stiff-solver episode recurring at `t=6.1843-6.1844e-7s`, agreeing to 6 significant figures, in all four `N1=3` cells regardless of `N2_PRECHARGE`) attributes this directly to the already-diagnosed R04E10/R04E11 retry/chatter artifact landing, for the first time in this project's lineage, transiently inside the admission window itself (longer admission chains are still running when the fixed-timing episode occurs) rather than a new construct defect -- corroborated by the static-analysis proof and by every cell still completing its expected rotations cleanly with `IL1-4` safely bounded (worst case `60.17A`, `24%` of the `+/-250A` bound; `IL2_max` shows a small real increase to `34.84A`, `13.9%` of the bound, at `N2_PRECHARGE>=3`). RESULT: stacking the phase-2 gate does NOT further improve `Vout`, `VC1`, or `VC3` at any tested `N2_PRECHARGE` -- all three are WORSE than the `N2_PRECHARGE=1` baseline (`Vout`: `-10.6%` to `-11.9%`; `VC1`: `-6.1%` to `-22.2%`; `VC3`: `-18.9%` to `-35.7%`). `VC2_final` shows only a small (`~4%`) improvement at `N2_PRECHARGE in {5,10}` (far smaller than R04E12's own phase-1-gate `VC2` gains), paired with a substantially larger `VC3` cost at those same values, and is markedly WORSE (`-33.2%`) at `N2_PRECHARGE=3`. The directly-identified root cause: adding ANY phase-2 gate costs exactly 2 completed rotations within the fixed `TSTOP=20us` window (`12->10`, regardless of `N2_PRECHARGE`'s exact value beyond 3) -- the one-time admission chain's own wall-clock cost eats into the window that would otherwise fund additional round-robin rotations, a dynamic R04E12's own phase-1-only gate never exhibited at any tested value. The R04E10/R04E11-diagnosed retry/chatter dynamic recurs in every cell at a modestly higher rate (`26-27%` for `N2_PRECHARGE>1` cells vs. `21.7-21.8%` for the two `N2_PRECHARGE=1` identity cells, matching R04E12's own `~19-22%` range) -- a longer admission chain providing more wall-clock opportunity for the same episode class, not a new phenomenon. No cell reaches handoff; no `PHYSICAL_BOUNDARY_FAIL` in any cell. | Citing that a naive, more-direct generalization of a validated one-time admission-gate construct to a SECOND, downstream gate position can fail (mirror chain re-triggers every rotation) even when the first gate's own construct was fully validated, and that the correct fix (a dedicated one-time "ADMIT" duplicate of every intervening loop state) is a genuinely stronger structural requirement than a first-position gate needs -- a concrete methodological lesson for any future multi-stage-gate construct on this `.machine` family; citing that stacking a second precharge gate downstream of a first, already-validated one can be net-negative within a fixed simulation-time window because of the added admission chain's own wall-clock/rotation-count cost, not just because of its own electrical effect, as a mechanism worth checking in any future stacked-gate design; citing the precisely-timed cross-cell evidence method (comparing the same stiff-episode timestamp across cells with different construct lengths) as a way to distinguish a genuine construct defect from a recurrence of an already-diagnosed solver artifact, for any future raw-trace pilot-check failure in this construct family; citing that this retry/chatter dynamic can, for a long enough admission chain, intrude directly into an admission-order verification itself, not only into ordinary round-robin dwells | That `N2_PRECHARGE=5` or `10` (or any tested value) is "optimal" or paper-derived (the swept axis is `SENSITIVITY_ONLY`); that this experiment closes or substantially narrows the handoff gap (best `Vout` still `98.5%` short of `1V`, the same structural per-cycle-convergence limitation R04E10 Section 10 already identified, unaddressed here); any P24 Vout/steady-state handoff (not attempted); a `Cout` correction (not attempted, same open gap as R04E7-R04E12); that stacking precharge gates is a general strategy independent of the specific `N1_PRECHARGE` value chosen (only `N1_PRECHARGE=3` was tested; the full `N1 x N2` interaction space is unexplored); that gating phases 3/4 would show the same net-negative result (untested); any further diagnosis of the `ICS1-3` artifact's LTspice-internals root cause (R04E11 already left this undiagnosed at that level; this experiment adds a new, precisely-timed admission-window-intrusion observation only, not a new root-cause finding); R04E9 Section 11's options (a) (direct topology change) or (b) (passive precharge circuit), neither evaluated here |

| R04E14 corrected-Cfly replay of the R02A/R02B passive-precharge module | `PASS_TOPOLOGY_PRINCIPLE` (2/14 cells), `NOT_P24_REPRODUCTION`, `SENSITIVITY_ONLY` (overall grid) | Returns to R02A/R02B's own original passive-divider-plus-diode precharge module (the IPEC-2018-derived circuit, structurally unrelated to the R04E9-R04E13 inductor-mediated `CHARGE_k`/`FREE_k` lineage) and re-tests R02A/B's own `FAILED_CAPACITANCE_TRANSFER` verdict against this project's own corrected, first-principles `Cfly` range instead of the cross-topology-suspect `53.8uF` EPE2019 value R02A/B used. A byte-level-faithful copy of R02B's own `.cir` circuit body (four equal input-divider capacitors, `LPAR=5nH`/`RPAR=10mOhm` (IPEC 2018 Table II), three ideal precharge diodes, ground-referenced flying-capacitor lower terminals, true-zero-energy `UIC` initial conditions, no PWM/switches/load) with ONLY `CFLY` and `CDIV`'s swept range changed: primary grid `CFLY=3uF` fixed (R04E8's own corrected value) x `CDIV` in `{10,30,100,300}uF` (re-scaled around R02A's own found `10:1` `CDIV:CFLY` ratio, replacing R02B's `{10,53.8,538,1076}uF` which was scaled for the old `53.8uF` target) x `TRAMP` in `{1,10,100}us` (unchanged from R02B) = 12 cells; secondary grid = the primary grid's own single best `(CDIV,TRAMP)` cell by `LADDER_ERR` (`CDIV=300uF`,`TRAMP=10us`, `LADDER_ERR=0.0351`) re-run at `CFLY=0.6uF` and `8.7uF` (the first-principles range's own extremes) = 2 cells. All 14 cells run as one netlist file per cell (a packaging difference from R02B's own multi-`.step` file, forced by an empirically-isolated LTspice-runner path-length failure mode: full absolute `.cir` paths at or above ~250-259 characters silently drop all `.meas` output with no error, `unable to open database file` in the `.log`; confirmed via direct binary-search isolation testing, worked around with short case filenames). RESULT: 2 of the 12 primary cells (`CDIV=100uF` and `CDIV=300uF`, both at `TRAMP=100us`) simultaneously reach a `LADDER_ERR` (`0.157`/`0.054`) at or below R02A's own best passive result (`Step 1`, `CFLY=1uF`, `LADDER_ERR~=0.236`) AND a peak source current (`16.45A`/`39.18A`) at or below R02B's own best (`150.15A`) -- both halves of `BOUNDARY.md`'s own explicit `PASS_TOPOLOGY_PRINCIPLE` bar, graded using R02A's own Step-1 language, not a new category. `9`/`12` primary cells beat R02B's own best `LADDER_ERR` (`0.260`); only the three `CDIV=10uF` cells do not (`0.963-1.106`), though even these remain better than R02B's own worst cell (`2.732`). Peak current is NOT uniformly rescued by the `Cfly` correction alone: the 12-cell span (`3.31-2497.83A`) overlaps R02B's own old-`Cfly` span (`6.01-3429.57A`), and three `TRAMP=1us` cells exceed the `~500A` flagged threshold (up to `2497.83A`) -- confirming R02B's own `CDIV`/`TRAMP` separability finding (final charge set by `CDIV`, peak current separately controllable by `TRAMP`) still holds at the corrected `Cfly`. The secondary grid shows `LADDER_ERR` is genuinely sensitive to which point in the `0.6-8.7uF` range is used at the fixed best `(CDIV,TRAMP)` point (`13x` spread, `0.0107` at `0.6uF` to `0.1389` at `8.7uF`, monotonic with `Cfly`), while peak current stays nearly flat (`8%` spread, `382.83-412.23A`) -- consistent with R04E8's own `V/Ron`-dominated peak-current finding in a structurally different, active ratio-gating mechanism, now confirmed in this older, purely passive module too. | Citing that R02A/B's own passive-divider-plus-diode module (not just the newer R04E7-E13 ratio-gating lineage) is rescued from `FAILED_CAPACITANCE_TRANSFER` by the corrected `Cfly` range at specific, identified `(CDIV,TRAMP)` points; citing the two identified PASS cells (`CDIV=100uF/TRAMP=100us` and `CDIV=300uF/TRAMP=100us`, both `CFLY=3uF`) as the best passive-divider-only ladder-plus-current result in this project to date; citing the `CDIV`/`TRAMP` separability and `Cfly`-insensitive-peak-current findings as confirming, in a second and structurally different mechanism, patterns R02B and R04E8 each found independently in their own; citing the isolated and documented LTspice-runner path-length failure mode (`~250-259` characters) as a tooling caution for any future experiment generating long, descriptive netlist filenames in a deeply nested worktree path | That any tested `CDIV`/`TRAMP`/`CFLY` combination is a "correct" or paper-derived component value (all are `SENSITIVITY_ONLY`); that the grid-wide result is a uniform pass (10/14 cells do not meet the Section-7 bar, most clearly the three `CDIV=10uF` cells and every `TRAMP=1us` cell); any P24 reproduction (the `N=4` divider topology remains IPEC 2018's own extension, generalized as a test hypothesis by R02A, unchanged and unvalidated here); any combination with PWM/the P24 switching stage (explicitly blocked pending the separate Roberts & Prodić 2024 literature review); reconciliation of this module's ground-referenced flying-capacitor convention with the floating adjacent-capacitor connection R04E9-R04E13's own active netlists use (inherited from R02A unchanged); which single point in the `0.6-8.7uF` range is the real component (still `SENSITIVITY_ONLY`, and Section 8's own sensitivity result shows the answer is not fully insensitive to this choice); any modification, overwrite, or invalidation of R02A/R02B's own committed results (unchanged, remain the correct record for the old `Cfly=53.8uF` value) |
| R04E15 fine-grid refinement of R04E14's own two PASS cells | `PASS_TOPOLOGY_PRINCIPLE` (12/13 cells), `NOT_P24_REPRODUCTION`, `SENSITIVITY_ONLY` (overall grid) | Fills three explicit gaps R04E14's own coarse 12-point grid left, on the SAME unmodified circuit (no mechanism, topology, or parameter-model change): Grid A, `CFLY=3uF`/`TRAMP=100us` fixed x `CDIV` in `{150,200,250,350,400,500}uF` (6 cells, fills the `100-300uF` gap and extends beyond `300uF`); Grid B, `CFLY=3uF`/`CDIV=300uF` fixed x `TRAMP` in `{20,50,75,150,200}us` (5 cells, fills the `10-100us` gap); Grid C, at the winning `(CDIV,TRAMP)` point identified from Grids A/B combined with R04E14's own already-committed `CDIV=300uF/TRAMP=100us` cell (the lowest `LADDER_ERR` among all candidates still satisfying `IIN_PK<=150.15A`: `CDIV=500uF/TRAMP=100us`, `LADDER_ERR=0.0335`, `IIN_PK=61.44A`), re-run at `CFLY=0.6uF` and `8.7uF` (2 cells) -- 13 cells total, all run as one netlist file per cell reusing R04E14's own short-filename convention (longest committed path `241` characters, confirmed clean, no path-length failure recurred). RESULT: no cell beats R04E14's own best PASS cell (`CDIV=300uF/TRAMP=100us`, `LADDER_ERR=0.0536`, `IIN_PK=39.18A`) on BOTH axes simultaneously -- `LADDER_ERR` decreases monotonically with `CDIV` through `500uF` (no plateau yet) while `IIN_PK` rises monotonically along with it, confirming R04E14's own coarse-grid optimum sits at or near a genuine local `LADDER_ERR`-vs-`IIN_PK` trade-off frontier, not an artifact of under-sampling; this experiment's own best-`LADDER_ERR`-while-passing-the-current-bar cell, `CDIV=500uF/TRAMP=100us`, improves `LADDER_ERR` by `37.6%` (`0.0536->0.0335`) at the cost of a `56.8%` higher `IIN_PK` (`39.18A->61.44A`, still `2.4x` under the bar) -- a legitimate alternative operating point, not a strict dominance; `12`/`13` cells (all except `CDIV=300uF/TRAMP=20us`, which fails only the current half of the bar at `195.90A`) simultaneously satisfy both halves of the `PASS_TOPOLOGY_PRINCIPLE` bar. Grid C's own `Cfly`-robustness check, run this time at the ACTUAL winning `(CDIV=500uF,TRAMP=100us)` point rather than R04E14's own `TRAMP=10us` secondary-grid point, passes BOTH halves of the bar at all three tested `Cfly` values (`0.6/3/8.7uF`): `LADDER_ERR` `0.0066-0.0956` (still far under `0.236`, though still monotonic-increasing with `Cfly` as R04E14 also found) and `IIN_PK` a nearly flat `60.48-63.68A` (far under `150.15A`) -- unlike R04E14's own secondary grid, where all three `Cfly` values FAILED the current bar outright (`382.83-412.23A`) regardless of `Cfly`, because that check was run at a different, current-bar-failing `TRAMP=10us` point. This shows the `PASS_TOPOLOGY_PRINCIPLE` verdict is genuinely `Cfly`-robust at the actual winning operating point, a materially stronger and more complete robustness result than R04E14 itself established, though specific to that one operating point. Every numeric claim was verified directly against each case's own raw `.log` `.meas` output before being recorded. | Citing `CDIV=500uF/TRAMP=100us/CFLY=3uF` as an alternative passive-divider-only PASS operating point with a materially better `LADDER_ERR` than R04E14's own best cell, at a peak current still well under R02B's own `150.15A` ceiling; citing the `LADDER_ERR`-vs-`IIN_PK` trade-off frontier this fine grid maps as evidence that R04E14's own coarse-grid optimum was not an under-sampling artifact; citing the Grid C result that the `PASS_TOPOLOGY_PRINCIPLE` verdict survives the full `0.6-8.7uF` `Cfly` first-principles range specifically AT THE `CDIV=500uF/TRAMP=100us` operating point; citing the general lesson that a `Cfly`-sensitivity check's informativeness depends on which `(CDIV,TRAMP)` operating point it is run at, since `TRAMP` (not `Cfly`) determines whether the current axis clears the bar at all | That any cell here strictly dominates R04E14's own best PASS cell (none does; every improvement on one axis costs the other); that any tested `CDIV`/`TRAMP`/`CFLY` combination is a "correct" or paper-derived component value (all remain `SENSITIVITY_ONLY`); that `Cfly`-robustness holds at every `(CDIV,TRAMP)` point in the grid (R04E14's own `CDIV=300uF/TRAMP=10us` point remains `Cfly`-sensitive AND current-bar-failing, not re-tested or changed here); any P24 reproduction (same unresolved `N=4`-topology and ground-referenced-flying-capacitor caveats R02A/R04E14 already carry, unchanged); any combination with PWM/the P24 switching stage (same standing Roberts & Prodić 2024 literature-review block as R02A/B/R04E14); any modification, overwrite, or invalidation of R04E14's own committed grid, results, or grading (unchanged, remain the correct record; this is a separately-numbered refinement) |

| R04E16 Roberts' dissertation soft-start mechanism on fixed-timing 4-phase PWM | `CONTROLLER_GUARD_PASS` (6/6 completed cells), `GRID_COMPLETE` (6/6 cells), `SENSITIVITY_ONLY` (overall, with characterized `Cfly` and margin-factor trends) | First build-and-run of Roberts' PhD dissertation Section 3.5 soft-start idea (`Vin` ramped slowly through an eFuse while the converter's ORDINARY, non-event-gated fixed-timing PWM runs unchanged from `t=0`) applied to P24 -- explicitly distinct from R04E5's own already-falsified "ramp grafted onto the strict event-gated controller" combination (this project's own standing README warning). Netlist copied from R03A's own gate-generation B-sources and four-phase power-stage connectivity verbatim (only `TSTART=0` changed in the formulas themselves), `LPHASE` corrected to this project's `1.4666667nH` standing value (was R03A's `2.68nH`), `CFLY` swept across the `0.6-8.7uF` first-principles range (was R03A's fixed `53.8uF`), and R02B's passive-divider precharge network removed entirely (true zero-energy start, no auxiliary precharge circuit). Grid: `Cfly` in `{0.6,3,8.7}uF` at Roberts' own `30x` margin (3 cells) x margin factor `{10x,100x}` at `Cfly=3uF` (2 cells) x a `Cfly=3uF/Tramp=1us` near-instantaneous-ramp control cell (1 cell) = 6 cells, `TSTOP=Tramp+300us` each. A pilot netlist (gate B-sources only) confirmed `TSTART=0` gating toggles correctly (`Ton`/`T`/`T/4`-phase-spacing all match to 4+ significant figures), with one negligible (~`5e-17s`) floating-point boundary artifact at `t=0` reported transparently. EVERY cell, first attempted with R03A's own default LTspice solver options (`reltol=1e-5 abstol=1e-9 chgtol=1e-16`, "Normal" solver), hit a reproducible, forensically-confirmed solver breakdown: direct byte-level `.raw` inspection showed classic near-duplicate-timestamp retry chatter (the same fingerprint documented for R04E10/R04E11's unrelated `.machine` construct) immediately followed by the INDEPENDENT `Vin` PWL source itself reading physically impossible voltages (`-20885V`, then `5.33e24V`), unambiguous proof of Newton-iteration corruption rather than a physical result, occurring while real circuit state was still completely benign (`IL1` `10-20A`) and at different absolute times for different `(Cfly,Tramp)`. Fixed by adopting `solver=alt cshunt=1e-15 plotwinsize=0`, the exact numerical-stabilization convention every one of R04E5-R04E10's own build scripts already uses at this project's own `TMAX=50ps` (`TMAX` itself never altered, at any point across all 6 cells). With the fix, all 6 cells eventually reached completion cleanly, but only across MULTIPLE sessions because of a SECOND, independent tooling finding -- severe, `TSTOP`-disproportionate runtime variance (`1302.6/1932.1/7937.0/11842.4/2165.2/2525.2s` of real LTspice compute time across the 6 cells, an `8.3x` spread in simulated-ns-per-wall-second that does NOT scale with `TSTOP` at all -- the two LARGEST-`TSTOP` cells finished FASTER than two smaller-`TSTOP` ones) plus a measured `~20x` throughput collapse when cells are run concurrently (each LTspice process requests `10` threads on this `10`-core machine); all 6 cells were therefore run strictly one at a time, never concurrently, each confirmed complete via its own `.log` `Total elapsed time` line before the next was launched. RESULT (6/6 cells): ALL SIX completed cells avoid R03A's own catastrophic runaway (`884.36/745.07/625.63/563.46A`, `Vout` overshoot to `1.54V`) -- max phase current `75.1-150.1A` across the six cells, `3.75x-7.51x` below R03A's own smallest peak, all comfortably inside the `+/-250A` safety bound, with NO `Vout` overshoot (opposite of R03A's `54%` overshoot). BUT the control cell (near-instantaneous `Tramp=1us` ramp) EQUALLY avoids the runaway (`IL1_max=150.07A`, no overshoot, final state `Vout=0.5588V`/`LADDER_ERR=1.345`, nearly identical to the `10x`/`30x`/`100x`-margin cells' own `Vout=0.5588-0.5589V`/`LADDER_ERR=1.339-1.348`) -- directly undermining a clean "the `Vin` ramp specifically is responsible" causal claim, exactly the possibility BOUNDARY.md's own Section 10 flagged as separately reportable if it occurred; this conclusion is unchanged by completing the grid. Most likely explanation: removing R02B's passive-divider precharge network (which is what let R03A's own ladder pre-charge to `34.0/21.8/10.6V` before full-strength PWM hit a still-cold `Vout` -- R03A's own diagnosed mismatch) eliminates the runaway mechanism independent of ramp speed. NEW with the completed grid: peak inductor current (`IL1_max`) now shows a clean MONOTONIC decrease with margin factor (`150.07/111.59/86.94/75.05A` at `Tramp=1/22.87/68.61/228.71us`, diminishing returns at each step) -- so the ramp does not determine WHETHER runaway occurs but does have a real, measurable, secondary effect on the transient's magnitude -- while `Vout_final`/`LADDER_ERR` remain essentially `Tramp`-insensitive across the full `229x` range. The completed 3-point `Cfly` sensitivity trend (`0.6/3/8.7uF`, all `30x` margin) shows `LADDER_ERR`/`Vout_final` decreasing modestly but monotonically with `Cfly` (`LADDER_ERR` `1.399->1.348->1.333`) while `IL1_max` is small and non-monotonic (`92.2/86.9/90.2A`). | Citing that removing R02B's passive-divider precharge network (independent of any `Vin`-ramp mechanism) avoids R03A's own diagnosed catastrophic-runaway failure mode, confirmed across all 6 tested points; citing the solver-corruption forensic evidence and its `solver=alt`/`cshunt` fix as a reusable diagnostic method and fix for any future fixed-timing, zero-dead-time-switch, many-period netlist in this project reaching similarly small `TMAX` values; citing the documented severe runtime-variance/concurrency-collapse tooling finding as a caution for any future experiment budgeting LTspice wall-clock time at `TMAX=50ps` over hundreds of switching periods; citing the confirmed, monotonic margin-factor sensitivity of peak transient current, and the modest, monotonic `Cfly` sensitivity of `LADDER_ERR`/`Vout_final`, as characterized (not merely flagged-as-untested) `SENSITIVITY_ONLY` trends | That the `Vin`-ramp mechanism itself (Roberts' own Sec. 3.5 idea) is confirmed responsible for WHETHER the benign outcome occurs (the control-cell counterfactual specifically undermines this, even though the ramp does measurably affect transient magnitude); any `LOCAL_PASS`-style convergence claim (all 6 completed cells reach only `~56%` of `Vout` target, `LADDER_ERR~=1.33-1.40`, far from the `<0.05-0.1` range other Track-B passive-divider experiments reached); any GS61008T device-level (`Coss`/dead-time/ZVS) validation (ideal switches only); which single `Cfly` or margin factor is "correct" (all remain `SENSITIVITY_ONLY`); any P24 reproduction claim of any kind (same standing Track-B limitation as R04E9-R04E15) |
| R04E17 factorial isolation of divider-removed vs. Vin-ramped | `SENSITIVITY_ONLY`, `NOT_P24_REPRODUCTION` (2/2 new cells completed) | Per explicit user direction, isolates the two simultaneous changes R04E16 could not separate (removing R02B's own passive-divider precharge network vs. ramping `Vin` slowly) via a `2x2` factorial design crossing `{divider present, absent}` x `{fast ramp `Tramp=1us`, slow ramp `Tramp=68.61us`}`. R04E16's own two already-completed divider-ABSENT cells (`e16_ctrl_f3_t1`, `e16_g1_f3_t68p61`) are reused strictly by reference (their already-published numbers copied verbatim, not re-run). Two NEW divider-PRESENT cells were built by grafting R03A's own divider-plus-diode network (`CIN1-4`/`RLEAK1-4`/`DPC1-3`, model `DPRE`, connecting into the SAME real `a1`/`a2`/`a3` power-stage nodes R03A itself used) onto R04E16's own otherwise-unmodified netlist (four-phase power stage, `TSTART=0`, `LPHASE=1.4666667nH`, `CFLY=3uF`, `solver=alt cshunt=1e-15 plotwinsize=0`/`TMAX=50p`, all unchanged), with `CDIV=300uF` (R04E15's own corrected best-point value at `Cfly=3uF`, NOT R03A's own cross-topology-suspect `1.076mF`). A truncated-`TSTOP` pilot copy confirmed the grafted network loads with no topology error before either full run. Both new cells were run strictly one at a time on real LTspice (never concurrently, per R04E16's own documented `~20x` concurrent-run throughput collapse on this machine), each confirmed complete via its own `.log` `Total elapsed time` line before the next was launched (`e17_div_f3_t1`: `1203.948s`; `e17_div_f3_t68p61`: `1641.488s`). Both new cells' raw `.raw` traces (`6,728,222` and `8,208,737` points respectively) were directly, byte-level parsed and confirmed free of R04E16's own documented solver-corruption fingerprint (zero non-monotonic/duplicate timestamps; the independent `V(vin)` PWL source never deviates from its own commanded ramp value anywhere in either trace). RESULT: the fast-ramp/divider-present cell (`e17_div_f3_t1`) shows genuine runaway -- phase currents `813.14/884.90/877.84/1046.42A` (`IL1-4_max`), all four individually exceeding the `+/-250A` bound, the same order of magnitude as R03A's own originally diagnosed `563-884A` runaway -- while the slow-ramp/divider-present cell (`e17_div_f3_t68p61`) does NOT (`IL1-4_max` `154.73/147.85/140.79/143.34A`, comfortably inside the bound, close to `e16_g1_f3_t68p61`'s own `86.94A` at the same `Tramp`). This is the clean third pattern of BOUNDARY.md Section 7: WITH the divider physically present, the `Vin` ramp speed is causally decisive, directly resolving R04E16's own open question in Roberts' own mechanism's favor for this specific comparison. Both divider-present cells also reach `Vout_final` within `0.6%` of the `1V` target and `LADDER_ERR` `0.0242-0.0291` -- `46-56x` better than either divider-ABSENT cell's own `LADDER_ERR~=1.34-1.35` -- so reintroducing the divider substantially improves ladder/output accuracy at EITHER tested ramp speed, not merely "safe if slow enough". | Citing that, with R03A's own divider network physically present at the corrected `CDIV=300uF`, a `Tramp=1us` ramp reproduces R03A's own catastrophic-runaway mechanism while a `Tramp=68.61us` ramp avoids it, cleanly isolated from the "divider removed" confound R04E16 could not separate; citing that reintroducing the divider (at either tested ramp speed) improves `LADDER_ERR`/`Vout_final` substantially over R04E16's own divider-ABSENT results; citing the confirmed-clean (fingerprint-free) forensic raw-trace check on both new cells as direct evidence the reported runaway current is a real transient result, not a numerical artifact | Any claim about `Cfly` values other than `3uF`, or `Tramp` values other than the two already tested (the runaway/no-runaway boundary between `1us` and `68.61us` is not located by this two-point design); any claim that R04E16's own committed 6-cell grid or `results.csv`/`results.json` are changed (unchanged, not re-run); any P24 reproduction claim of any kind (same standing Track-B limitation as every other experiment in this lineage) |
| R04E18 localizing the divider-present runaway/safe `Tramp` boundary | `SENSITIVITY_ONLY`, `NOT_P24_REPRODUCTION` (3/3 new cells completed) | Adds three new `Tramp` points (`5us`, new; `22.87us`, R04E16's own already-derived `10x`-margin value, tested divider-PRESENT for the first time; `40us`, new, filling the `10x`/`30x`-margin gap) to R04E17's own unmodified divider-present netlist (`Cfly=3uF`, `CDIV=300uF`, `solver=alt cshunt=1e-15 plotwinsize=0`/`TMAX=50p`, nothing else changed), to map the runaway/safe transition R04E17 left completely unlocalized between its own two tested endpoints (`Tramp=1us` runaway, `68.61us` safe). R04E17's own two endpoint cells are reused strictly by reference (not re-run). All three new cells were run strictly one at a time on real LTspice (never concurrently), each confirmed complete via its own `.log` `Total elapsed time` line before the next was launched (`e18_div_f3_t5`: `1179.814s`; `e18_div_f3_t22p87`: `1341.366s`; `e18_div_f3_t40`: `1655.129s`). All three new cells' raw `.raw` traces (`6,815,063`/`7,211,500`/`7,589,536` points respectively) were directly, byte-level parsed and confirmed free of the documented solver-corruption fingerprint (zero non-monotonic/duplicate timestamps; `V(vin)` stayed within a physically plausible `0-48.36V` range against a commanded `0-48V` rail in all three, far from the documented corruption signature's many-orders-of-magnitude-impossible values). RESULT: `e18_div_f3_t5` (`Tramp=5us`) still shows runaway-scale current (`max{|IL1-4|}=651.55A`, all four phases over the `+/-250A` bound); `e18_div_f3_t22p87` and `e18_div_f3_t40` are both safe (`205.07A`/`170.41A`). This localizes the runaway/safe crossing against the `+/-250A` bound to the `5-22.87us` sub-interval, narrower than and entirely inside R04E17's own untested `1-68.61us` gap. The complete 5-point `max{|IL1-4|}` sequence (`1046.42/651.55/205.07/170.41/154.73A` at `Tramp=1/5/22.87/40/68.61us`) is smoothly monotonic-decreasing across the whole range with no discontinuous jump -- so while the `+/-250A` bound happens to be crossed within `5-22.87us`, the underlying transient-current relaxation with `Tramp` is gradual, not a sharp cliff; `Vout_overshoot`/`IIN_PK` show the same gradual pattern while `LADDER_ERR`/`Vout_final`/`VC1-3_final` stay essentially flat (`Tramp`-insensitive) across all 5 points. | Citing that, with the divider present at `Cfly=3uF`/`CDIV=300uF`, the runaway/safe boundary against the `+/-250A` bound localizes to `Tramp` between `5us` and `22.87us`; citing the confirmed gradual (monotonic, non-sharp) nature of the underlying `max{|IL1-4|}`/`Vout_overshoot`/`IIN_PK` trend across the full `1-68.61us` range as characterized evidence against a "sharp cliff" model of this transition; citing the confirmed-clean (fingerprint-free) forensic raw-trace check on all three new cells as direct evidence the reported currents are a real transient result, not a numerical artifact | Any precise, continuous threshold value (only a 5-point picture; the informal `~17.6us` log-log interpolation offered in `RESULTS.md` Section 5 is explicitly not an established value); any claim about `Cfly` values other than `3uF`; any claim that R04E16's or R04E17's own committed results are changed (unchanged, not re-run); any P24 reproduction claim of any kind (same standing Track-B limitation as every other experiment in this lineage) |
| R04E19 Cfly sensitivity of the divider-present runaway/safe boundary | `SENSITIVITY_ONLY`, `NOT_P24_REPRODUCTION` (4/4 cells completed, mixed/partial result) | Tests whether R04E18's own `Cfly=3uF` runaway/safe boundary (`5-22.87us`) shifts with `Cfly` the way the underlying `1/sqrt(Cfly)` resonance-frequency law predicts. Reuses R04E18's own already-tested `Tramp=5us` (unsafe at `Cfly=3uF`) at `Cfly=0.6uF`, and `Tramp=22.87us` (safe at `Cfly=3uF`) at `Cfly=8.7uF`, plus each `Cfly`'s own `30x`-margin model recommendation (`30.68us`/`116.84us` from `ROBERTS_SOFTSTART_P24_MODEL_DERIVATION.md`) -- four cells total, `CDIV=300uF` held fixed (not re-optimized per `Cfly`), everything else unchanged from R04E17/R04E18's own netlist. All four cells run strictly sequentially on real LTspice, confirmed complete via `.log` before the next was launched (`1305.7s`/`1471.1s`/`1375.5s`/`1639.4s`); the third and fourth cells were separated by an explicit user-directed pause (not a technical failure -- no cell was left mid-run), and the fourth cell was later run to completion in the same already-existing worktree by the orchestrating session directly, reusing the first three cells' already-invested compute. All four cells' raw `.raw` traces (`6.8`/`7.4`/`7.2`/`9.3` million points) were directly, byte-level parsed and confirmed free of the documented solver-corruption fingerprint. RESULT: the runaway/safe picture is genuinely more complex than a clean scaling law, per-phase asymmetric (unlike `Cfly=3uF`'s own phase-symmetric pattern) rather than a single crossing value. Using the corrected `max(|IL_min|,IL_max)` per-phase measure (a first analysis pass using only `IL_max` understated one cell's hazard, caught and corrected): `Cfly=0.6uF`/`Tramp=5us` remains unsafe on ALL FOUR phases (`412/321/398/1022A`, phase 4 WORSE than the `Cfly=3uF` reference's own `652A`), contradicting the "smaller Cfly = safer" direction of the prediction; `Cfly=8.7uF`/`Tramp=22.87us` becomes unsafe on 2 of 4 phases (`372/225/167/300A`), consistent with the "larger Cfly needs more margin" direction. Neither `30x`-margin cell is comfortably safe: `Cfly=8.7uF`'s (`116.84us`) is uniformly safe (`170/141/132/158A`); `Cfly=0.6uF`'s (`30.68us`) has phase 4 marginally over `250A` (`251.0A`). Separately, `Cfly=8.7uF` reaches substantially better `LADDER_ERR` (`0.009-0.019`) than `Cfly=0.6uF` (`0.134-0.138`); the `116.84us` cell's own `LADDER_ERR=0.00947` is the best value in this project's entire Track-B lineage to date. | Citing that the runaway/safe boundary DOES shift with `Cfly`, but as a per-phase-asymmetric pattern, not a single clean threshold; citing the large-`Cfly`-needs-more-margin direction of the resonance prediction as qualitatively supported; citing that the small-`Cfly`-tolerates-less-margin direction is NOT supported by this data (explicitly flagged, not glossed over); citing `e19_f8p7_t116p84`'s own `LADDER_ERR=0.00947` as this project's best Track-B ladder-accuracy result to date | That this experiment isolates the pure `1/sqrt(Cfly)` resonance law from the simultaneously-changing `CDIV/Cfly` charge-sharing ratio (`CDIV` was deliberately held fixed at `300uF`, so `CDIV/Cfly` changes from `500` to `~34.5` across the two tested `Cfly` values -- this experiment's own explicit, unresolved attribution limit); why phase symmetry breaks down away from `Cfly=3uF` (unexplained, not diagnosed further); a precise `Cfly`-vs-boundary-`Tramp` curve (only two `Cfly` points at two `Tramp` values each); any claim that R04E16/R04E17/R04E18's own committed results are changed (unchanged, not re-run); any P24 reproduction claim of any kind |
| R04E20 divider charge-conservation law across CDIV | `SENSITIVITY_ONLY`, `NOT_P24_REPRODUCTION` (4/4 new cells completed, mixed/partial result) | Prospectively tests a parallel, independently-developed hybrid-DAE math-model effort's own first-principles charge-conservation law (`Idiv=(CDIV/4)*Vin/Tramp`, predicting a runaway/safe threshold `Tramp_threshold=CDIV*Vin/(4*250A)`) at two `CDIV` values (`100uF`, `500uF`) it was not fitted to -- the law was previously only checked retrospectively against R04E18's own already-observed `CDIV=300uF` data. `Cfly=3uF` held fixed throughout (the reverse of R04E19's own design, directly avoiding R04E19's own `CDIV`/`Cfly`-ratio attribution confound), otherwise the SAME unmodified R04E17/R04E18 netlist (four-phase power stage, `TSTART=0`, `LPHASE=1.4666667nH`, `solver=alt cshunt=1e-15 plotwinsize=0`/`TMAX=50p`, R03A's own divider-plus-diode network). Four new cells at `0.5x`/`2x` each new `CDIV`'s own predicted threshold (`e20_c100_t2p4`, `e20_c100_t9p6`, `e20_c500_t12`, `e20_c500_t48`), plus R04E18's own two `CDIV=300uF` cells (`e18_div_f3_t5`, `e18_div_f3_t22p87`) reused strictly by reference, not re-run. All four new cells run strictly sequentially on real LTspice, confirmed complete via `.log` `Total elapsed time` before the next was launched (`1263.5s`/`1189.8s`/`1372.0s`/`1606.8s`), never concurrently. All four cells' raw `.raw` traces (`6.7`/`6.9`/`7.0`/`7.8` million points) were directly, byte-level parsed and confirmed free of the documented solver-corruption fingerprint (zero non-monotonic timestamps; `V(vin)` stayed within `0-50.3V` against the commanded `0-48V` rail in all four, the largest deviation `4.77%`, far below the documented corruption signature). RESULT (genuinely mixed, using the corrected `max(|IL_min|,IL_max)` per-phase hazard measure): `e20_c100_t2p4` (`CDIV=100uF`, `Tramp=2.4us`, predicted unsafe) confirms dramatically (`851/884/855/942A`, all four phases over `250A`); `e20_c100_t9p6` (`CDIV=100uF`, `Tramp=9.6us`, predicted safe) does NOT confirm -- still unsafe on all four phases (`332/288/261/309A`), a clear quantitative failure of the law at this `CDIV`; `e20_c500_t12` (`CDIV=500uF`, `Tramp=12us`, predicted unsafe) matches only marginally -- just one of four phases (`IL4=318.07A`) exceeds `250A`; `e20_c500_t48` (`CDIV=500uF`, `Tramp=48us`, predicted safe) confirms cleanly (`149/155/144/143A`, all four phases comfortably safe). The law's qualitative DIRECTION (larger `CDIV` needs more `Tramp` margin) holds at both new `CDIV` values, but the specific threshold formula is not an accurate quantitative predictor away from `CDIV=300uF`, increasingly so at the smaller `CDIV`. A plausible but NOT independently verified explanation is offered (a roughly `CDIV`-independent switching-stage current contribution becoming proportionally more significant relative to the shrinking divider term as `CDIV` drops) as a hypothesis for the parallel math-model effort, not an established finding. All six rows (new and reused) show the same phase-symmetric pattern R04E17/R04E18 already found at `CDIV=300uF` (unlike R04E19's own per-phase-asymmetric `Cfly`-sweep result), supporting (not proving) that divider charge demand is a more phase-uniform driver than the `Cfly` resonance mechanism. | Citing that the charge-conservation law's qualitative direction (larger `CDIV` needs more `Tramp` margin) is supported at `CDIV=100uF` and `500uF`, not merely at the originally-fitted `CDIV=300uF`; citing that the law's SPECIFIC threshold formula is confirmed NOT to be an accurate quantitative predictor at `CDIV=100uF` (explicitly flagged, not glossed over); citing the confirmed-clean (fingerprint-free) forensic raw-trace check on all four new cells as direct evidence the reported currents are real, not numerical artifacts; citing the observed phase-symmetric pattern across all six rows as evidence distinguishing the divider-charge-demand mechanism from R04E19's own resonance-driven, phase-asymmetric pattern | That the charge-conservation law is a precise quantitative predictor of the runaway/safe boundary at any `CDIV` other than `300uF` (explicitly refuted at `CDIV=100uF`); the specific "fixed non-divider current contribution" explanation offered for the observed pattern (a plausible, unverified hypothesis only); the exact location of the runaway/safe crossing at `CDIV=100uF` (known only to be beyond `9.6us`) or `CDIV=500uF` (known only to be between `12us` and `48us`); any claim that R04E16/R04E17/R04E18/R04E19's own committed results are changed (unchanged, not re-run); any P24 reproduction claim of any kind |
| R04E21 phase-1 handoff into R04E3's own documented stall | `SENSITIVITY_ONLY`, `NOT_P24_REPRODUCTION`, negative result (same stall recurs) | Explicitly NOT a P24-periodicity test (a 2026-09-18 research pass, recorded in `R04E21_phase1_handoff_into_r04e3_stall/BOUNDARY.md` Section 0, found no genuinely closed periodic orbit anywhere in this project by any method; independently unresolved regardless of this experiment). Tests one narrow question: does R04E3's own event-gated phase-1 local-commutation controller (`paper_locked/02_ectc2024_main/spice/R04E3_P24_minimal_zero_start_event_cycle.cir`, unmodified except `Cfly` corrected `53.8uF->3uF` per R04E8's own precedent) avoid its own documented permanent stall at state `COMMUTATE_HIGH_TO_ZVS` (waiting forever for `V(vin,a1)<=0`) when phase 1 starts from R04E17's own real bootstrapped state (`e17_div_f3_t68p61`'s own `VC1`/`IL1` at `TSTOP=368.61us`) instead of true zero energy. Step 1 re-ran R04E17's own unmodified cell with one added `.meas` line to obtain the missing instantaneous `IL1_AT_TSTOP=75.96527862548828A` (R04E17's own committed set only had the `IL1_MIN`/`IL1_MAX` envelope); the re-run's own `.log` failed to print `.meas` results (a benign post-processing hiccup, not a simulation failure -- the `.raw` trace's own point count, `8,208,737`, exactly matches R04E17's own already-published count), so all values were independently re-extracted directly from the `.raw` trace and cross-checked: all 18 of R04E17's own already-published `.meas` values reproduced exactly before `IL1_AT_TSTOP` was trusted. Step 2 built the new cell with `VC1_IC=35.85894624845418V`, `IL1_IC=75.96527862548828A` on phase 1 only (`CS1`, `L1`); phases 2-4 deliberately kept R04E3's own hardcoded zero-energy, single-phase-isolation gate configuration and `ic=0` (an explicit, stated scope limit -- their own real R04E17-bootstrapped currents were discarded, not fed in; a genuine four-phase handoff is explicitly not attempted here). A truncated-`TSTOP` pilot confirmed the netlist loads with no topology error before the full run (`Total elapsed time: 35.444s`, as expected for R04E3's own short `TSTOP=20us` local-commutation timescale). RESULT: the SAME stall recurs -- `STATE_FINAL=4` (`COMMUTATE_HIGH_TO_ZVS`) at `TSTOP=20us`; `T_HIGH_SIDE_ZVS` explicitly `FAIL`s to trigger, identical to R04E3's own zero-start cell and R04E5's own confirmed `600us`-persistent version of the same stall. The bootstrapped cell reaches this same parked state far faster (`~1.69us`, vs. needing to first build `IL1` from `0` to `I_LIMIT=10A`) but does not avoid it, directly answering this experiment's own narrow question in the negative: bootstrapped current/voltage magnitude alone, at the handoff instant, is not sufficient to unblock the high-side ZVS event. Phase 1's own current stayed within the `+/-250A` bound throughout (`max|IL1|=75.967A`, `0` of `193,606` scanned points over bound). The `.raw` trace (`193,606` points) was directly, byte-level parsed and confirmed free of the documented solver-corruption fingerprint (zero non-monotonic/duplicate timestamps; `V(vin)` stayed exactly within its commanded `0-48V` step). Neither R04E3's nor R04E17's own committed files/results were modified. | Citing that bootstrapping phase 1 alone with R04E17's own real state does not unblock R04E3's own documented `COMMUTATE_HIGH_TO_ZVS` stall, so raw current/voltage magnitude at the handoff instant is confirmed NOT to be the blocking factor (some other trajectory/phase-relationship property must be); citing the confirmed-clean (fingerprint-free) forensic raw-trace check as direct evidence the reported stall is a real controller-logic result, not a numerical artifact; citing the exact re-verification of all 18 of R04E17's own published `.meas` values (to full precision) as confirmation this experiment's own Step-1 re-run is a faithful, unmodified reproduction | Any P24 periodicity claim of any kind (explicitly out of scope, Section 0); any four-phase handoff claim (phases 2-4 kept R04E3's own hardcoded, zero-energy, single-phase-isolation configuration throughout, not their own real bootstrapped states, Section 4); any claim that R04E3's own or R04E17's own committed results are changed (unchanged, not re-run); any claim that this validates R04E3's own controller as "the" P24 steady-state control law (R04E3 itself is a phase-1-only local-commutation test, not a P24 control-law reproduction); any claim about what happens past state 5 (`BLOCK_P24_HANDOFF_UNKNOWN`) since state 5 was never reached here either |

## Reuse rule

Before a downstream experiment imports any result above, its header must name
the source experiment and copy both the permitted use and the prohibited claim.
Raw waveforms alone are never a parameter source.
