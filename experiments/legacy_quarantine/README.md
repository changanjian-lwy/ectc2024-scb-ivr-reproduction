# Legacy quarantine index

Files are not moved or deleted because existing reports may link to them.

- `project/circuit/ectc2024_verified_4phase_module.net`: not currently verified;
  uses 8 modules/125 W, fitted duty 0.102, assumed components and complementary
  low-side PWM.
- other `experiment_I0*` and `R01*` files: historical exploration only unless a
  current paper-locked STEP record explicitly promotes one.
